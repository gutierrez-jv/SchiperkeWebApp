@model VaccinationRecordFormViewModel

@{
    ViewData["Title"] = "Create Vaccination Record";
}

<div class="page-header">
    <div>
        <div class="page-kicker">Preventive Care</div>
        <h1>Create Vaccination Record</h1>
        <p class="page-subtitle">Record vaccines, date given, next due date, and optional batch details.</p>
    </div>
</div>

<form asp-action="Create" method="post">
    <partial name="_Form" model="Model" />
    <div class="mt-4 form-actions">
        <button type="submit" class="btn btn-primary">Save</button>
        <a asp-action="Index" class="btn btn-outline-secondary">Back to List</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
