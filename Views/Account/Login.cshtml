@model LoginViewModel

@{
    ViewData["Title"] = "Login";
}

<section class="auth-layout">
    <div class="auth-panel">
        <div class="page-kicker">Secure Workspace</div>
        <h1>Login</h1>
        <p class="auth-copy">Authorized clinic users can manage appointments, patient records, and medical workflows.</p>

        <form asp-action="Login" method="post" class="mt-4">
            <input asp-for="ReturnUrl" type="hidden" />
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="Username" class="form-label"></label>
                <input asp-for="Username" class="form-control" autocomplete="username" />
                <span asp-validation-for="Username" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Password" class="form-label"></label>
                <input asp-for="Password" class="form-control" autocomplete="current-password" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Sign In</button>
                <a asp-controller="Appointments" asp-action="Book" class="btn btn-outline-secondary">Book Appointment</a>
            </div>
        </form>
    </div>

    <aside class="auth-aside">
        <h2 class="h4">Clinic workspace</h2>
        <p>Use your assigned account to review bookings and update patient-centered clinic records.</p>
    </aside>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
